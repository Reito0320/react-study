# Your local PostgreSQL. See chapter basic-07 or docs/prisma-local.md.
# Docker example uses learner / local_learning_only / port 5433.
# Create a separate hook_build database first.
DATABASE_URL="postgresql://YOUR_USER:YOUR_PASSWORD@127.0.0.1:5432/hook_build"
